# ═══════════════════════════════════════════════════════════════
# Security Gates Aggregator
# Runs all S1-S8 security gates in sequence
# ═══════════════════════════════════════════════════════════════

name: '🔒 Security Gates (S1-S8)'

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

permissions:
  contents: read
  checks: write

jobs:
  # ═══════════════════════════════════════════════════════════════
  # S1: Environment Verification (Existing)
  # ═══════════════════════════════════════════════════════════════
  s1-gate:
    name: 'S1 Environment Verification'
    uses: ./.github/workflows/s1-compliance.yml
    
  # ═══════════════════════════════════════════════════════════════
  # S2: Tenant Isolation Gate (NEW)
  # ═══════════════════════════════════════════════════════════════
  s2-gate:
    name: 'S2 Tenant Isolation'
    uses: ./.github/workflows/s2-tenant-isolation-gate.yml
    
  # ═══════════════════════════════════════════════════════════════
  # S6: Rate Limiting Gate (NEW)
  # ═══════════════════════════════════════════════════════════════
  s6-gate:
    name: 'S6 Rate Limiting'
    uses: ./.github/workflows/s6-rate-limiting-gate.yml
    
  # ═══════════════════════════════════════════════════════════════
  # S7: Encryption Gate (NEW)
  # ═══════════════════════════════════════════════════════════════
  s7-gate:
    name: 'S7 Encryption at Rest'
    uses: ./.github/workflows/s7-encryption-gate.yml

  # ═══════════════════════════════════════════════════════════════
  # Final Security Report
  # ═══════════════════════════════════════════════════════════════
  security-summary:
    name: 'Security Gates Summary'
    runs-on: ubuntu-latest
    needs: [s1-gate, s2-gate, s6-gate, s7-gate]
    if: always()
    steps:
      - name: Report Security Status
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🔒 SECURITY GATES SUMMARY"
          echo "═══════════════════════════════════════════════════════════════"
          echo ""
          echo "S1 - Environment Verification: ${{ needs.s1-gate.result }}"
          echo "S2 - Tenant Isolation:         ${{ needs.s2-gate.result }}"
          echo "S6 - Rate Limiting:            ${{ needs.s6-gate.result }}"
          echo "S7 - Encryption at Rest:       ${{ needs.s7-gate.result }}"
          echo ""
          
          # Check if any gate failed
          if [ "${{ needs.s1-gate.result }}" == "failure" ] || \
             [ "${{ needs.s2-gate.result }}" == "failure" ] || \
             [ "${{ needs.s6-gate.result }}" == "failure" ] || \
             [ "${{ needs.s7-gate.result }}" == "failure" ]; then
            echo "🚨 CRITICAL: One or more security gates FAILED"
            echo "═══════════════════════════════════════════════════════════════"
            exit 1
          fi
          
          echo "✅ ALL SECURITY GATES PASSED"
          echo "═══════════════════════════════════════════════════════════════"
          echo ""
          echo "Implemented Security Protocols:"
          echo "  ✓ S1: Environment Verification - Fail-fast on invalid config"
          echo "  ✓ S2: Tenant Isolation - Cross-tenant access blocked"
          echo "  ✓ S6: Rate Limiting - DDoS/Brute force protection"
          echo "  ✓ S7: Encryption at Rest - Sensitive data encrypted"
          echo ""
          echo "Pending Protocols (S3, S4, S5, S8):"
          echo "  • S3: Input Validation (Zod validation on endpoints)"
          echo "  • S4: Audit Logging (all sensitive operations logged)"
          echo "  • S5: Exception Handling (no stack trace leakage)"
          echo "  • S8: Security Headers (CSP, HSTS, Helmet)"
          echo "═══════════════════════════════════════════════════════════════"
