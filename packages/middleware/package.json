{
  "name": "@apex/middleware",
  "version": "1.0.0",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --outDir ./dist --declaration --declarationMap --sourceMap && echo 'Build completed'",
    "test": "vitest run",
    "test:unit": "vitest run --coverage",
    "lint": "biome check .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@apex/config": "workspace:*",
    "@apex/db": "workspace:*",
    "@nestjs/common": "^10.0.0",
    "drizzle-orm": "^0.29.0",
    "express": "^4.18.0",
    "redis": "^4.6.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "bun-types": "latest",
    "vitest": "^1.0.0",
    "@biomejs/biome": "^1.5.0",
    "@types/express": "^4.17.21",
    "typescript": "^5.3.0"
  }
}
